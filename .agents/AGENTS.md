# Agent Behavioral Rules

## Automated Git Commit and Push Protocol
Whenever a user task or coding request is completed, automatically stage all changes, commit them with a clear, descriptive commit message, and push the changes to the remote Git repository (`git push origin <branch>`).
