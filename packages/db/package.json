{
  "name": "@bucketspace/db",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "prisma:generate": "prisma generate",
    "type-check": "tsc --noEmit",
    "test": "tsc --project tsconfig.test.json && node --test dist/test/sqlite-metadata-repository.test.js"
  },
  "dependencies": {
    "@bucketspace/shared": "workspace:*",
    "@prisma/client": "^5.16.0"
  },
  "devDependencies": {
    "prisma": "^5.16.0",
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
